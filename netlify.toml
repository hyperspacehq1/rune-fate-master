[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"
  external_node_modules = ["pg"]

# ✅ Optional: environment variable injection (if needed)
[build.environment]
  NODE_VERSION = "18"